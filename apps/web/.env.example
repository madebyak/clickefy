# Clickefy Web — environment variables
# Front-end only for now; these are stubs for the integration phase.

# Base URL of the Clickefy Worker API (used once the studio talks to the backend)
NEXT_PUBLIC_API_URL=https://api.clickefy.ai

# Auth (Clerk) — add when the studio app goes behind login
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
# CLERK_SECRET_KEY=

# Billing (Stripe) — add during the monetization phase
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
